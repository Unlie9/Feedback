repos:
  - repo: local
    hooks:

      - id: ruff
        name: ruff
        entry: ruff check --fix
        language: python
        types: [python]
        stages: [pre-commit]
        always_run: true

